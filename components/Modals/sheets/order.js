import { View, Text } from 'react-native'
import React from 'react'
import Maps from '../../navigaton/reusable/Maps'

export default function Order({ previewDelivery, setPreviewDelivery }) {
    return (
        <View>
            <Maps />
        </View>
    )
}